FAZA 1: Podstawowa Strona Statyczna (Priorytet 1) ⏱️ 1-2 tygodnie
=============================
Milestone 1.1: Setup projektu

# Inicjalizacja
npx create-next-app@latest michal-bukala-portfolio --typescript --tailwind --app
cd michal-bukala-portfolio

# Instalacja podstawowych dependencies
npm install framer-motion clsx tailwind-merge date-fns
npm install -D @types/node

Deliverables:

✅ Next.js project setup
✅ Tailwind configured
✅ TypeScript configured
✅ Folder structure created
---------------------------

Milestone 1.2: Layout & Navigation

Deliverables:

✅ Responsive header z nawigacją
✅ Footer z kontaktami
✅ Mobile menu (hamburger)
---------------------------

Milestone 1.3: Strony statyczne
Kolejność implementacji:

About page (src/app/about/page.tsx)

Przeniesienie treści z about.md
Photo + bio
Publications page (src/app/publications/page.tsx)

Lista publikacji (bez globu na razie)
Filtering/sorting
Publication cards
Projects page (src/app/projects/page.tsx)

Research projects
Funding information
Collaboration page (src/app/collaboration/page.tsx)

Contact form lub informacje
Deliverables:

✅ 4 główne strony działające
✅ Responsive design
✅ SEO metadata
----------------------------

Milestone 1.4: Deployment na GitHub Pages

# .github/workflows/deploy.yml
name: Deploy to GitHub Pages

on:
  push:
    branches: [main]

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      
      - name: Setup Node
        uses: actions/setup-node@v3
        with:
          node-version: '18'
          
      - name: Install dependencies
        run: npm ci
        
      - name: Build
        run: npm run build
        env:
          NEXT_PUBLIC_BASE_PATH: /micbukala.github.io
          
      - name: Export
        run: npm run export
        
      - name: Deploy
        uses: peaceiris/actions-gh-pages@v3
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./out

next.config.js:
/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export',
  basePath: process.env.NODE_ENV === 'production' ? '/micbukala.github.io' : '',
  images: {
    unoptimized: true,
  },
}

module.exports = nextConfig

Deliverables:

✅ Strona live na GitHub Pages
✅ CI/CD pipeline działa
✅ Custom domain (opcjonalnie)